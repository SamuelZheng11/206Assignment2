package commandLineExecutor;

/**
 * this interface is concerned with defining all methods that something that listens for whether or not
 * the commands have been executed should have
 *
 */
public interface CommandLineRunnerListener {
	public void commandsHaveBeenExecuted();
}
